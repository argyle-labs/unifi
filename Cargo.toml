[package]
name = "unifi"
version = "0.1.0"
edition = "2021"
description = "UniFi Network controller integration — orca appliance backend"

[package.metadata.orca]
category = "networking"

[lib]
name = "unifi"
path = "src/lib.rs"
crate-type = ["rlib"]

[[bin]]
name = "unifi"
path = "src/main.rs"

# unifi is a BACKEND-ONLY orca plugin: zero #[orca_tool]s. It registers a
# ServiceBackend over the orca socket and reaches the whole orca system
# through plugin-toolkit alone.
[dependencies]
plugin-toolkit = { git = "https://github.com/argyle-labs/orca", branch = "main" }

[dev-dependencies]
plugin-toolkit = { git = "https://github.com/argyle-labs/orca", branch = "main" }
tokio    = { version = "1", features = ["macros", "rt-multi-thread"] }
tempfile = "3"

[lints.clippy]
let_underscore_must_use = "deny"
